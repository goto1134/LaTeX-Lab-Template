sudo: required
dist: trusty
before_install:
- sudo apt-get -qq update && sudo apt-get install -y texlive-fonts-recommended texlive-latex-extra texlive-fonts-extra dvipng texlive-latex-recommended latexmk
script:
- latexmk -pdf -synctex=1 "LabWork"
#deploy:
#  provider: releases
#  api_key:
#    secure: [YOUR KEY]
#  file:
#  - _build/your_file_1.pdf
#  - _build/your_file_2.pdf
#  skip_cleanup: true
#  on:
#tags: true
